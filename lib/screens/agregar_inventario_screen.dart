import 'package:flutter/material.dart';

class AgregarInventarioScreen extends StatelessWidget {
  const AgregarInventarioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Agregar Inventario')),
      body: const Center(child: Text('Aquí va la pantalla de Agregar Inventario')),
    );
  }
}
