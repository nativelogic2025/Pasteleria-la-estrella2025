import 'package:flutter/material.dart';

class VerPedidosScreen extends StatelessWidget {
  const VerPedidosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Pedidos')),
      body: const Center(child: Text('Aquí va la pantalla de Ver Pedidos')),
    );
  }
}
